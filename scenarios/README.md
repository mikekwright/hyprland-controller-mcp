# Scenarios

This area is owned by `scenario-validator`.

Contents:

- `definitions/` for scenario definitions
- `fixtures/` for scenario inputs and expected artifacts
- `runs/` for run records
- `artifacts/` for screenshots, recordings, logs, and manifests
- `reports/` for validator summaries

Validation artifacts are runtime evidence, not implementation code.
